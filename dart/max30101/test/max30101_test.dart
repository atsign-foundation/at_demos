import 'package:test/test.dart';

// TODO Add tests for:
// * BitwiseOperators
// * Register Utils
// * I2C Wrapper
// * Max30101 calculations (capture readings from a live run, play back into a test and assert outputs)
void main() {
  group('A group of tests', () {
    setUp(() {
      // Additional setup goes here.
    });

    test('First Test', () {
      expect(1 == 1, isTrue);
    });
  });
}
